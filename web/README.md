# ForecastHub Web

React + Vite dashboard for ForecastHub.

## Local Run

```bash
cd web
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_BASE_URL` to your Render API URL in Vercel project settings.
